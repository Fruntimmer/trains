using UnityEngine;
using System.Collections;

public class GUIScript : MonoBehaviour
{
    /*
    private CreateRailScript rScript;
    private float bHeight = 50;
    private float bWidth = 100;
    private float lHeight = 0;
    private Train selectedTrain;
	// Use this for initialization
	void Start ()
	{
	    rScript = GameObject.Find("Plane").GetComponent<CreateRailScript>();
	}
	
	// Update is called once per frame
	void Update () {
	
	}
    void OnGUI()
    {
        if(rScript.t.Count > 0)
        {
            foreach (Train train in rScript.t)
            {
                if (GUI.Button(new Rect(750, 250+lHeight,bWidth,bHeight ), "Train"))
                {
                    selectedTrain = train;
                    Debug.Log("Selected train: "+selectedTrain);
                    
                }
                lHeight += bHeight;
            }
        }
        lHeight = 0;
    }
    */
}
